torch
torchvision
ultralytics
sahi
anomalib==1.2.0
requests
pyyaml
numpy
pandas
opencv-python
pillow
scikit-learn
matplotlib
tqdm

# D-FINE and RT-DETR pull in their own training dependencies (transformers,
# pycocotools, etc.) via their upstream repos — see models/dfine/train_dfine.py
# (--dfine-repo, expects a vendored checkout of https://github.com/Peterande/D-FINE)
# and models/rtdetr/train_rtdetr.py (uses ultralytics' RTDETR).
